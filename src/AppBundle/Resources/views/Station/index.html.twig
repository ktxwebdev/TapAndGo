<h1>Station list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Address</th>
            <th>Lat</th>
            <th>Long</th>
            <th>Capacity</th>
            <th>Numberofbikeavailable</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for document in documents %}
        <tr>
            <td><a href="{{ path('city_id_station_show', { 'id': document.id }) }}">{{ document.id }}</a></td>
            <td>{{ document.name }}</td>
            <td>{{ document.address }}</td>
            <td>{{ document.lat }}</td>
            <td>{{ document.long }}</td>
            <td>{{ document.capacity }}</td>
            <td>{{ document.numberOfBikeAvailable }}</td>
            <td>{{ document.status }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('city_id_station_show', { 'id': document.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('city_id_station_edit', { 'id': document.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('city_id_station_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
