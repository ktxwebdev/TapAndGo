<h1>Station</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ document.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ document.name }}</td>
        </tr>
        <tr>
            <th>Address</th>
            <td>{{ document.address }}</td>
        </tr>
        <tr>
            <th>Lat</th>
            <td>{{ document.lat }}</td>
        </tr>
        <tr>
            <th>Long</th>
            <td>{{ document.long }}</td>
        </tr>
        <tr>
            <th>Capacity</th>
            <td>{{ document.capacity }}</td>
        </tr>
        <tr>
            <th>Numberofbikeavailable</th>
            <td>{{ document.numberOfBikeAvailable }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ document.status }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('city_id_station') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('city_id_station_edit', { 'id': document.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('city_id_station_delete', { 'id': document.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
